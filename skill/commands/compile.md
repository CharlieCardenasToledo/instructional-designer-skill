# `/jintia compile`

Compila una guía con `pdflatex` y `biber`. Antes ejecuta automáticamente el
hook `pre-compile` para bloquear errores deterministas:

```bash
npx jintia compile semanas/semana-03/latex/guia-semana-03.tex
```
